{
    "experiment": {
        "text": "${data.text}_ol", 
        "entity_type": "${data.entity_type}"
    },
    "evaluation": {
        "evaluation": {
            "ground_truth": "${data.results}",
            "entities": "${run.outputs.entities}"
        }
    }

}